# 决策层审计意见（针对 v1）

## 审计结论
- 结论：**不通过，需返工**。

## 主要问题
1. **合规边界不清**：未明确“仅处理支付回执，不处理清结算”的责任边界。
2. **账务不完备**：缺少冻结、解冻、冲正、退款状态机。
3. **风险不足**：缺少防重放、防重复回执、防并发超扣方案。
4. **观测不足**：缺少全链路追踪ID和审计事件分类。
5. **生态不足**：未体现政府试点与大厂接入的低摩擦方案。

## 修改指令（必须落实）
- 增加 `Ledger State Machine` 设计，覆盖全状态迁移。
- 增加 `Idempotency + Nonce + Signature` 机制。
- 增加 `Policy Layer`，输出监管报表和试点报表。
- 增加 `Partner Sandbox SDK`，降低接入门槛。
- 对“所有权转移”改为“收益分成权转移”并加争议冻结流程。
